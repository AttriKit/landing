---
/**
 * Docs Index Page
 * Main documentation landing page
 */
import { getCollection } from 'astro:content';
import '../../styles/global.css';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import ConsultationModal from '../../components/ConsultationModal.astro';
import PagefindSearch from '../../components/PagefindSearch.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';

// Get all published docs
const allDocs = await getCollection('docs');
const publishedDocs = allDocs.filter(doc => !doc.data.draft);

// Group by category
const categoryMap = new Map<string, typeof publishedDocs>();

for (const doc of publishedDocs) {
  const category = doc.data.category;
  if (!categoryMap.has(category)) {
    categoryMap.set(category, []);
  }
  categoryMap.get(category)!.push(doc);
}

// Sort docs within each category by order
for (const docs of categoryMap.values()) {
  docs.sort((a, b) => a.data.order - b.data.order);
}

// Category metadata
const categoryInfo: Record<string, { name: string; description: string; icon: string }> = {
  'getting-started': {
    name: '快速开始',
    description: '了解 AttriKit 产品并快速完成集成',
    icon: '🚀'
  },
  'guides': {
    name: '使用指南',
    description: '深入学习归因原理和最佳实践',
    icon: '📖'
  },
  'api': {
    name: 'API 参考',
    description: '完整的 API 接口文档',
    icon: '🔧'
  },
};

// Category order
const categoryOrder = ['getting-started', 'guides', 'api'];
---

<BaseLayout
  title="产品文档 - AttriKit 第一方广告归因系统"
  description="AttriKit 完整产品文档，涵盖快速开始、私有化部署指南、归因配置说明和 API 参考文档。帮助您在 1 小时内完成第一方归因系统的搭建和集成。"
  canonical="/docs/"
>
  <!-- Header -->
  <Header />

  <!-- Main Content -->
  <main class="docs-index-page">
    <!-- Hero Section -->
    <section class="docs-hero">
      <div class="container">
        <div class="docs-hero-content">
          <h1 class="docs-hero-title">产品文档</h1>
          <p class="docs-hero-description">
            快速了解 AttriKit 产品功能，掌握归因分析方法，提升广告投放 ROI
          </p>
        </div>
        <PagefindSearch type="docs" scope="main" placeholder="搜索文档..." resultLabel="篇文档" />
      </div>
    </section>

    <!-- Docs Categories -->
    <section class="docs-categories">
      <div class="container">
        {categoryOrder
          .filter(cat => categoryMap.has(cat))
          .map(category => {
            const info = categoryInfo[category];
            const docs = categoryMap.get(category)!;

            return (
              <div class="docs-category-section">
                <div class="docs-category-header">
                  <span class="docs-category-icon">{info?.icon || '📄'}</span>
                  <div>
                    <h2 class="docs-category-name">{info?.name || category}</h2>
                    <p class="docs-category-description">{info?.description}</p>
                  </div>
                </div>

                <div class="docs-grid">
                  {docs.map(doc => (
                    <a href={`/docs/${doc.slug}/`} class="doc-card">
                      <h3 class="doc-card-title">{doc.data.title}</h3>
                      <p class="doc-card-description">{doc.data.description}</p>
                      <span class="doc-card-link">阅读更多 →</span>
                    </a>
                  ))}
                </div>
              </div>
            );
          })}
      </div>
    </section>
  </main>

  <!-- Footer -->
  <Footer />

  <!-- Consultation Modal -->
  <ConsultationModal />

<style>
  .docs-index-page {
    padding-top: var(--header-height);
  }

  /* Hero Section - Compact */
  .docs-hero {
    padding: var(--spacing-10) 0 var(--spacing-8);
    background: linear-gradient(135deg, rgba(10, 95, 143, 0.03) 0%, rgba(0, 180, 166, 0.03) 100%);
    border-bottom: 1px solid var(--color-border);
  }

  .docs-hero-content {
    text-align: left;
  }

  .docs-hero-title {
    font-size: clamp(28px, 4vw, 40px);
    font-weight: 700;
    font-family: var(--font-family-display);
    color: var(--color-text-primary);
    margin: 0 0 var(--spacing-3) 0;
    letter-spacing: -0.02em;
  }

  .docs-hero-description {
    font-size: var(--font-size-base);
    color: var(--color-text-muted);
    max-width: 500px;
    margin: 0;
    line-height: 1.6;
  }

  /* Search box alignment */
  .docs-hero :global(.search-trigger-container) {
    margin: var(--spacing-6) 0 0;
    max-width: 320px;
  }

  /* Docs Grid */
  .docs-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: var(--spacing-5);
  }

  /* Categories Section */
  .docs-categories {
    padding: var(--spacing-16) 0;
  }

  .docs-category-section {
    margin-bottom: var(--spacing-16);
  }

  .docs-category-section:last-child {
    margin-bottom: 0;
  }

  .docs-category-header {
    display: flex;
    align-items: center;
    gap: var(--spacing-4);
    margin-bottom: var(--spacing-8);
  }

  .docs-category-icon {
    font-size: 40px;
    line-height: 1;
  }

  .docs-category-name {
    font-size: var(--font-size-2xl);
    font-weight: 600;
    color: var(--color-text-primary);
    margin: 0 0 var(--spacing-1) 0;
  }

  .docs-category-description {
    font-size: var(--font-size-base);
    color: var(--color-text-muted);
    margin: 0;
  }


  /* Doc Card */
  .doc-card {
    display: block;
    padding: var(--spacing-8);
    background: var(--color-bg-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    text-decoration: none;
    transition: all var(--transition-base);
  }

  .doc-card:hover {
    border-color: var(--color-accent-primary);
    box-shadow: var(--shadow-md);
  }

  .doc-card-title {
    font-size: var(--font-size-lg);
    font-weight: 600;
    color: var(--color-text-primary);
    margin: 0 0 var(--spacing-3) 0;
  }

  .doc-card-description {
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    margin: 0 0 var(--spacing-4) 0;
    line-height: 1.5;
  }

  .doc-card-link {
    font-size: var(--font-size-sm);
    color: var(--color-accent-primary);
    font-weight: 500;
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .docs-hero {
      padding: var(--spacing-8) 0 var(--spacing-6);
    }

    .docs-hero-title {
      font-size: var(--font-size-2xl);
    }

    .docs-hero-description {
      font-size: var(--font-size-sm);
      max-width: 100%;
    }

    .docs-hero :global(.search-trigger-container) {
      margin: var(--spacing-5) 0 0;
      max-width: 100%;
    }

    .docs-category-header {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--spacing-2);
    }

    .docs-grid {
      grid-template-columns: 1fr;
    }

    .doc-card {
      padding: var(--spacing-6);
    }
  }

  @media (max-width: 480px) {
    .docs-hero {
      padding: var(--spacing-12) 0;
    }

    .docs-categories {
      padding: var(--spacing-12) 0;
    }
  }
</style>
</BaseLayout>
